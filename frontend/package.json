{
  "name": "daysleft-app",
  "version": "1.0.0",
  "description": "DaysLeft - Ressourcen-Tracker für Overlander",
  "private": true,
  "scripts": {
    "build": "rm -rf www && mkdir -p www && cp offgrid.html www/index.html && cp -r css www/ && cp -r js www/ && cp -r i18n www/ && cp manifest.json www/ && cp sw.js www/",
    "cap:sync": "npm run build && npx cap sync",
    "cap:ios": "npm run build && npx cap sync ios && npx cap open ios",
    "cap:android": "npm run build && npx cap sync android && npx cap open android",
    "cap:run:ios": "npm run build && npx cap run ios",
    "cap:run:android": "npm run build && npx cap run android"
  },
  "dependencies": {
    "@capacitor/core": "^8.1.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/network": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/ios": "^8.1.0",
    "@capacitor/android": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "typescript": "^5.0.0"
  }
}
